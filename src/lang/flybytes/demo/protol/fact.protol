
math = new {
  fact(n) {
    if (n <= 1) {
      return 1;
    }
    else {
      return n * this.fact(n - 1);
    }
  }
  
  missing(name, args) {
    print name;
    print args;
    return name;
  }
};

print math.fact(6);

print math.fibo(2,3);