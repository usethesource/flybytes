
math = new {
  fact(n) {
    if (n <= 1) {
      return 1;
    }
    else {
      return n * this.fact(n - 1);
    }
  }
};

print "fact(6) = " << math.fact(6);

six = new 6 {
  fact(math) {
    return math.fact(this);
  }
};

print "6.fact(math) = " << six.fact(math);

